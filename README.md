# codeception-mediabuyers

Assumptions:
- Content-Type header has charset=utf-8 defined
- Authorization happens with an API key
- Schemas are auto-generated, they might get overridden
- Initials are required in POST according to schema, but not according to Field validation rules (Assuming validation rules are correct)
